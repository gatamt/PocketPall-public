import SwiftUI

/// No longer used — ESP32 handles WiFi network scanning.
/// Kept as empty stub to avoid dangling file references in Xcode project.
struct ScanDevicesView: View {
    var body: some View {
        Text("Not available")
            .foregroundStyle(.gray)
    }
}
